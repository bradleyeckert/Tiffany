v1 testbench compiles a definition to flash and "see"s it.

things to do next:
1. Add UART hardware access to flash's xport.
2. Port to an FPGA board.
3. Write an app to re-flash over serial.

